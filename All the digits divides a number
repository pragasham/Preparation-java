// User function Template for Java

class Solution {
    static int evenlyDivides(int n) {
        int dup=n;
        int count=0;
        while(n!=0)
        {
            int rem=n%10;
            if(rem!=0 && dup%rem==0) count++;
            n/=10;
        }
        return count;
    }
}
