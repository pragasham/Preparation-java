
class Solution {
    static boolean armstrongNumber(int n) {
        // code here
        int tem=n;
        int sum=0;
        while(n!=0)
        {
            int rem=n%10;
            sum+=(rem*rem*rem);
            n/=10;
        }
        return tem==sum ? true:false;
    }
}
