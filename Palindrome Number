class Solution {
    public boolean isPalindrome(int n) {
        // code here
        int tem=n;
        int re=0;
        while(n>0)
        {
            int rem=n%10;
            re=re*10+rem;
            n/=10;
        }
        return re==tem?true:false;
    }
}
